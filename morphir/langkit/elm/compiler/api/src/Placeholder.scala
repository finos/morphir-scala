// Skeleton-verification placeholder — proves source/module wiring compiles for jvm/js/native.
// Deleted once the real krueger `compiler-api` port lands.
package morphir.langkit.elm.compiler.api

private[api] val placeholder: Unit = ()
