// Skeleton-verification placeholder — proves source/module wiring compiles for jvm/js/native.
// Deleted once the real krueger `core` port lands.
package morphir.langkit.elm.core

private[core] val placeholder: Unit = ()
