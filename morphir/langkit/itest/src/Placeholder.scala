// Skeleton-verification placeholder — proves source/module wiring compiles.
// Deleted once the real krueger `itest` port lands.
package morphir.langkit.itest

private[itest] val placeholder: Unit = ()
