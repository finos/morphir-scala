// Skeleton-verification placeholder — proves source/module wiring compiles for jvm/js/native.
// Deleted once the real krueger `trees` port lands.
package morphir.langkit.trees

private[trees] val placeholder: Unit = ()
