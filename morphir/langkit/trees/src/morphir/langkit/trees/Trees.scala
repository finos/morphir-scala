package morphir.langkit.trees

/**
 * Entry point for the langkit.trees module.
 *
 * Hosts the generic `QueryableTree[T]` typeclass and the tree-sitter-inspired query DSL.
 */
object Trees:
  val moduleName: String = "morphir-langkit-trees"
