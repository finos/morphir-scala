package org.finos.morphir.universe
import org.finos.morphir.testing.munit.*
import org.finos.morphir.universe.MorphirTypeTag

class MorphirTypeTagTestSuite extends MorphirTestSuite {
  describe("MorphirTypeTag") {
    describe("Instances") {
      describe("for org.finos.morphir.sdk.dsl") {
        import org.finos.morphir.sdk.dsl.*
        // test("An instance for sdk.dsl.Basics.Int should exist") {
        //   val tag = MorphirTypeTag[Basics.Int]
        //   assert(tag != null)
        // }
      }
    }
  }
}
