$schema: "https://moonrepo.dev/schemas/tasks.json"
tasks:
  build:
    deps:
      - target: "~:morphir-elm-build"
        optional: true
      - target: "~:elm-library-build"
        optional: true
  fmt:
    deps:
      - target: "~:elm-format"
        optional: true
      - target: "~:scala-format"
        optional: true

  lint:
    deps:
      - target: "~:elm-lint"
        optional: true
      - target: "~:scala-lint"
        optional: true
