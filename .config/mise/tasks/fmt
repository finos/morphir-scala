#!/usr/bin/env bash
# MISE description="Format Scala code"
# MISE alias="format"

set -e

echo "Formatting code..."
echo "  Formatting Scala files..."
# This used to hang against the broad 'morphir.__.sources' glob; see the note in
# the `lint` task for why, and why the old --no-server workaround made it worse
# rather than better.
./mill mill.scalalib.scalafmt.ScalafmtModule/reformatAll 'morphir.__.sources'
echo "Format completed successfully"
