#!/usr/bin/env bash
# MISE description="Check the mirrored schemas, and validate every mirrored v4 document against them"
# MISE depends=["schemas:build"]

set -uo pipefail

# Squire reports schema metaschema findings and validates every complete mirrored document. Known upstream v4
# incompatibilities remain findings rather than a daily gate, matching the policy this task has always enforced.
.claude/skills/squire/squire schemas validate "$@"
